# 정부지원사업 필터링 알고리즘 가이드

업데이트 시 이 문서를 먼저 읽고, `filter_rules.py`를 수정하세요.

---

## 판단에 쓰는 텍스트 두 종류

`judge()`는 공고 텍스트를 두 갈래로 나눠서 씁니다. 어떤 목록에 키워드를 추가할지
정할 때 **그 목록이 어느 텍스트를 보는지** 먼저 확인하세요.

| 이름 | 구성 | 쓰는 곳 |
|------|------|---------|
| `region_text` | 제목 + 지원대상 + **소관기관명** | 지역 판단 (기관명이 '화성시청'처럼 지역 신호가 됨) |
| `content_text` | 제목 + 지원대상 | 지원대상·행사여부·창업전용·업종·관심분야 판단 |

**기관명을 뺀 이유**: 기관명에는 업종·행사 키워드가 우연히 들어갑니다.
`공공연연구인력파견지원`이 업종 키워드 '공연'에 걸려 진짜 적합한 공고가
부적합이 되거나, '고용'이 '고용노동부'에 걸려 무관한 공고가 적합이 되는 식입니다.

---

## 판단 흐름

```
공고 수집
  │
  ▼
[1] 지원대상 제외 키워드 (EXCLUSION_KEYWORDS, content_text)
    1인기업·여성기업전용·농업인·기관 자체 채용공고 등 → 부적합
  │
  ▼
[2] 소상공인 전용 (_is_small_business_only, content_text)
    '소상공인'이 있고 '중소기업'이 함께 없으면 → 부적합
  │
  ▼
[3] 지원사업이 아닌 행사·교육·시상 (NON_SUPPORT_KEYWORDS, content_text)
    밋업·박람회·경진대회·우수성과 모집·국비교육 등 → 부적합
  │
  ▼
[4] 창업 초기기업 전용 (STARTUP_ONLY_KEYWORDS, content_text)
    팁스(TIPS)·창업보육·창업스쿨 등 → 부적합 (당사는 업력 7년 초과)
  │
  ▼
[5] 지역 필드 체크 (_mentions_company_region)
    region 필드가 당사 소재지(서울/경기)와 무관한 지역이면 → 부적합
  │
  ▼
[6] 광역 단위 타 지역 감지 (_title_implies_non_company_region, region_text)
    NON_COMPANY_REGIONS 있고 COMPANY_REGION_KEYWORDS 없으면 → 부적합
  │
  ▼
[7] 시·군·구 단위 타 지역 감지 (_implies_other_district, region_text)
    OTHER_DISTRICTS 있고 COMPANY_DISTRICTS·전국·수도권 없으면 → 부적합
  │
  ▼
[8] 업종 블랙리스트 (_check_industry_fit, content_text)
    INDUSTRY_BLACKLIST 키워드가 하나라도 있으면 → 부적합
  │
  ▼
[9] 관심분야 키워드 (interest_keywords, config.py, content_text)
    R&D·채용·시설·융자 등 매칭 → 적합
  │
  ▼
[10] 위 모두 통과했지만 관심 키워드 없음 → 모호
```

---

## 주요 목록 관리 위치 (`filter_rules.py`)

### 지역 관련
| 목록 | 역할 |
|------|------|
| `NON_COMPANY_REGIONS` | 타 광역 지역 키워드 (경북·경남·영남·동해시 등) |
| `COMPANY_REGION_KEYWORDS` | 당사 해당 광역 (전국·수도권·서울·경기·화성) |
| `OTHER_DISTRICTS` | 당사 사업장이 없는 수도권 시·군·구 (성남시·시흥시·서초구 등) |
| `COMPANY_DISTRICTS` | 당사 사업장이 있는 시·군·구 |
| `WIDE_REGION_KEYWORDS` | 시·군 이름이 있어도 구제하는 광역 표현 (전국·수도권) |

**추가 시점**: 새로운 타 지역 도시/자치구/광역 표현이 누락됐을 때.
서울·경기 공고라도 특정 시·군·구 소재 기업만 신청 가능한 경우가 많아
`OTHER_DISTRICTS` 누락이 오탐의 가장 큰 원인입니다.

### 지원대상 관련
| 목록 | 역할 |
|------|------|
| `EXCLUSION_KEYWORDS` | 당사 해당 불가 대상 + 기관 자체 채용·인사 공지 |
| `NON_SUPPORT_KEYWORDS` | 자금 지원이 아닌 행사·교육·시상 공고 |
| `STARTUP_ONLY_KEYWORDS` | 창업 초기기업(대개 7년 이내) 전용 공고 |

**추가 시점**: 지원금이 아닌 공지가 '적합'으로 올라올 때.

### 업종 관련
| 목록 | 역할 |
|------|------|
| `INDUSTRY_BLACKLIST` | 무관 업종 (방산·뷰티·바이오·반도체·문화체육 등) |

**업데이트 원칙**:
- 새 무관 업종이 적합/모호로 뜨면 → `INDUSTRY_BLACKLIST`에 추가
- 키워드는 **업종 특정성이 높은 단어**로 (예: "공연예술" > "공연").
  짧은 단어는 다른 단어 안에 우연히 들어갑니다.
- 예전에는 `INDUSTRY_WHITELIST`가 블랙리스트보다 우선이었으나,
  '방사선 기반 소재ㆍ장비'가 '장비' 때문에 통과되는 문제가 있어 제거했습니다.

### 관심분야 키워드
`config.py`의 `COMPANY_PROFILE["interest_keywords"]`에서 관리.
GitHub에 올라가지 않는 비공개 파일이므로 GitHub Secrets(`COMPANY_PROFILE_JSON`)도 함께 수정 필요.

---

## 보안 주의

판단 사유(`reason`) 문구는 공개 GitHub Pages에 그대로 노출됩니다.
**회사 식별정보(회사명·상시인력수·설립연도·구체적 소재지)를 사유 문장에 넣지 마세요.**
"당사 조건과 맞지 않음"처럼 일반화된 표현만 씁니다.

---

## Slack 알림 조건 (`main.py`)

- **영업일에, '적합' 신규 공고가 있을 때만** 발송합니다.
  '모호'만 있는 날은 조용히 넘어갑니다.
- **주말·공휴일에는 발송하지 않습니다** (`business_day.py`).
  공휴일은 `holidays` 패키지가 계산하므로 설·추석 같은 음력 공휴일과
  대체공휴일까지 자동 반영되고, 매년 목록을 손볼 필요가 없습니다.
  GitHub Actions 러너는 UTC라서 날짜는 항상 KST로 변환해서 판단합니다.
- 주말·공휴일에 나온 '적합' 공고는 **`pending_notifications.json` 대기열에 쌓였다가
  다음 영업일에 함께 나갑니다** (`pending_store.py`).
  그냥 건너뛰면 그 공고는 `seen_announcements.json`에 '이미 본 공고'로 기록돼서
  다음 영업일에도 신규로 잡히지 않고 알림 없이 묻히기 때문입니다.
  대기열은 영업일 실행 때마다 (발송 여부와 무관하게) 비워집니다.
- 메시지 본문에는 '적합' 공고 제목만 최대 5건, 그 아래 '모호' 건수를 한 줄로 붙입니다.
- 수집·월별 페이지 생성은 알림 발송 여부와 무관하게 **매일** 그대로 돌아갑니다.

---

## 자주 생기는 케이스와 해결법

| 증상 | 원인 | 해결 |
|------|------|------|
| 경남·경북 공고가 적합으로 뜸 | 제목에 지역명 없고 region 필드 None | `NON_COMPANY_REGIONS`에 해당 도시명 추가 |
| 서울·경기인데 다른 시·군 공고가 적합으로 뜸 | `OTHER_DISTRICTS`에 해당 시·군 미등록 | 해당 시·군·구 추가 |
| 방산·우주항공 공고가 모호로 뜸 | `INDUSTRY_BLACKLIST` 키워드 미등록 | 해당 키워드 추가 |
| 관련 있는 공고가 갑자기 부적합이 됨 | 짧은 키워드가 다른 단어 안에 우연히 매칭 | 해당 키워드를 더 긴 표현으로 교체 |
| 기관명이 타 지역인데 통과됨 | agency 필드 비어있음 (스크래퍼 수정 필요) | 해당 source 클라이언트의 agency 파싱 확인 |

---

## 2단계 계획 (미착수)
- LLM(Claude API) 2차 판단 추가: `filter_rules.judge()`와 동일 입출력으로 `llm_judge.py` 작성 후 끼워넣기
- 경기/화성 지역 사이트 추가: gbsa.or.kr, hsbiz.or.kr
- 고용24 지원사업 전용 API 추가
